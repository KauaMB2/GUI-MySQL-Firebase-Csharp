# GUI-MySQL-Firebase-Csharp
This repository have a Graphical User Interface(GUI) that allows you choice between MySQL and Firebase for perform a CRUD. Every data is shown in a table. I used CSharp and .NET. 
